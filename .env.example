# Copy to .env and replace YOUR_PASSWORD with your database password.
# Password: Supabase Dashboard → Project "landing page" → Settings → Database
# → Database password (use "Reset database password" if you don't have it).
#
# Project ref: dgatyklbmbgtgdxxekdx
# Direct connection (psql, pgAdmin, migrations):
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.dgatyklbmbgtgdxxekdx.supabase.co:5432/postgres

# Optional: URL-encode the password if it contains @ # / etc., e.g. p@ss → p%40ss
